
/**
 * 使用循环来判断位数
 * 使用while 或 do-while的区别是do-while一定做一遍，while可以一次都不做
 * 
 * while的语句其实和if很像，无非if只做一次，而while可以做无数次，所有while一定要有改变条件的数值的机会。否则编程可以正确，但是逻辑可能错误
 * 人脑如何验证正确错误，采用将变量列出来后，将变量循环，结果进行对比。推荐使用特值法进行检验。使用比较边界的值，使用0，最大值最小值比较。
 *
 * 调试的方法有多种，其中可以选择打断点的方式或者将值printf后观察
 *
 * 编程难在想问题，小套路就是经常看别人写的代码。
 * 编程的方法往往有很多种，并不唯一。
 *
 */

{
    while(i>10){
        // bulabula;
        // ll
    }
}

{

    do
    {
        // bulabula;

    }while(i>10);

}
