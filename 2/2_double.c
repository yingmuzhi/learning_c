
/**
 * 关于浮点数的计算
 * 如果整数和浮点数放在一起计算，会优先将整数全部转换成浮点数再计算
 * double foot; // 输出时候，不论你printf是%lf %f，默认都是以double类型进行输出。但是由于浮点数是编码形式，所以输入时候要明确是%lf--double, %f--float
 *      scanf("%lf %lf", &foot, &inch);
 *      printf("ur double var is %lf or %f\n", foot, foot);
 */
